import { describe, it, expect } from "vitest";

// members can reassign container to another member
// managers cannot reassign container to another organization
// reassigning a container to another member should also reassign all equipment in the container to that member

// placeholder
describe("Reassign Container Tests", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
