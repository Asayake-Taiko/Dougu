describe("Organization Membership Tests", () => {
  it("should have a placeholder test", () => {
    expect(true).toBe(true);
  });
});

// only a manager should be able to delete a membership
// successfully delete a membership
// manager cannot delete memberships for a different organization
// manager cannot delete their own membership
